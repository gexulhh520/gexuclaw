核心结论：不要改原始 userMessage；也不要把 base64 图片塞进 tool message。应该生成一个 llmToolResult，把 visualObservation 合并到对应的 role:"tool" 消息里，同时单独落一条 observation step。

你仓库当前 ChatMessage.content 还是 string，需要改成支持 Kimi 的多模态数组。
当前 AgentRuntime 里工具结果是完整 jsonStringify(decoratedToolResult.toolResult) 塞给 LLM，这里必须改。

# 修改任务：MCP 图片结果不要撑爆 LLM 上下文，并自动生成视觉摘要继续工具链

## 背景

当前 AgentRuntime 工具执行后，会把完整 ToolResult 直接作为 `role:"tool"` 消息喂回 LLM：

```ts
messages.push({
  role: "tool",
  tool_call_id: toolCall.id,
  content: jsonStringify(decoratedToolResult.toolResult),
});

如果 MCP 工具返回图片类型结果，图片通常是很长的 base64，直接进入 ToolResult / messages 会导致 LLM 上下文超限。

现在需要改成：

MCP 图片 base64 不直接塞给 LLM。
ToolResult 对 LLM 只暴露轻量结果和图片引用。
Runtime 检测到工具结果有图片后，临时调用一次 VisionModel。
VisionModel 使用 Kimi 的多模态 image_url 格式理解图片。
VisionModel 输出结构化视觉摘要 visualObservation。
把 visualObservation 合并进本次 role:"tool" 消息里的 llmToolResult。
同时落库一条 stepType:"observation"，用于 trace / 前端展示 / 复盘。
不要把视觉摘要追加到原始 userMessage。
不要永久把 image_url/base64 放入 messages，只保留文字视觉摘要。
修改目标链路

最终运行链路应该是：

assistant tool_call
  ↓
ToolRuntime.execute()
  ↓
MCP 返回 image base64
  ↓
McpToolBridge 生成 rawToolResult：
    - 不把完整 MCP raw result 放进 data.result
    - 图片放 artifactCandidates / imageRefs
  ↓
AgentRuntime 检测 toolResult 有 image artifact
  ↓
调用 VisionModel：
    - 临时构造 Kimi image_url 消息
    - 得到 structured visualObservation
  ↓
构造 llmToolResult：
    - 轻量 ToolResult
    - visualObservation
    - 不包含 base64
  ↓
messages.push(role:"tool", content: jsonStringify(llmToolResult))
  ↓
子 Agent 下一轮根据 visualObservation 继续调用工具
涉及文件

主要修改：

agent-platform-node/src/runtime/model-client.ts
agent-platform-node/src/runtime/openai-compatible-provider.ts
agent-platform-node/src/modules/plugins/mcp/mcp-tool-bridge.ts
agent-platform-node/src/runtime/agent-runtime.ts

建议新增：

agent-platform-node/src/runtime/tool-result-llm-view.ts
agent-platform-node/src/runtime/vision-observation-builder.ts
一、修改 model-client.ts：支持多模态 ChatMessage

当前：

export type ChatMessage = {
  role: "system" | "user" | "assistant" | "tool";
  content: string;
  ...
};

改成：

export type TextContentPart = {
  type: "text";
  text: string;
};

export type ImageUrlContentPart = {
  type: "image_url";
  image_url: {
    url: string;
  };
};

export type ChatContent = string | Array<TextContentPart | ImageUrlContentPart>;

export type ChatMessage = {
  role: "system" | "user" | "assistant" | "tool";
  content: ChatContent;
  tool_call_id?: string;
  tool_calls?: Array<{
    id: string;
    type: "function";
    function: {
      name: string;
      arguments: string;
    };
  }>;
  reasoning_content?: string;
};

同时修改 invokeMock 里对 latestUser / latestTool / systemMessage 的处理，因为现在 content 可能是数组。

新增辅助函数：

function contentToText(content: ChatContent | undefined): string {
  if (!content) return "";

  if (typeof content === "string") {
    return content;
  }

  return content
    .filter((part) => part.type === "text")
    .map((part) => part.text)
    .join("\n");
}

然后把：

const latestUser =
  [...input.messages].reverse().find((message) => message.role === "user")?.content ?? "";

const latestTool =
  [...input.messages].reverse().find((message) => message.role === "tool")?.content ?? "";

const systemMessage =
  input.messages.find((message) => message.role === "system")?.content ?? "";

改成：

const latestUser = contentToText(
  [...input.messages].reverse().find((message) => message.role === "user")?.content
);

const latestTool = contentToText(
  [...input.messages].reverse().find((message) => message.role === "tool")?.content
);

const systemMessage = contentToText(
  input.messages.find((message) => message.role === "system")?.content
);
二、修改 openai-compatible-provider.ts：content 直接透传

当前 normalizeMessages 里是：

const normalized: Record<string, unknown> = {
  role: message.role,
  content: message.content ?? "",
};

这个可以保留，关键是不要把 message.content 强转 string。

确保 content 可以是：

[
  {
    type: "image_url",
    image_url: {
      url: "data:image/png;base64,..."
    }
  },
  {
    type: "text",
    text: "请描述这个图片"
  }
]

也就是 normalizeMessages() 直接透传 message.content。

三、修改 mcp-tool-bridge.ts：解析 MCP image，不把 raw image 放进 data.result

当前 normalizeMcpSuccessResult() 会把完整 MCP raw result 放入：

data: {
  result: mcpResult,
  textContent,
}

如果 mcpResult.content 里有 image base64，这里会很大。请修改成：

提取 text content。
提取 image content。
data 里只放轻量 imageRefs。
artifactCandidates 里保存 image candidate。
不再把完整 mcpResult 原样放进 data.result。

请把 MCP content 类型定义成：

type McpContentItem = {
  type: string;
  text?: string;
  data?: string;
  mimeType?: string;
};

修改 normalizeMcpSuccessResult() 为类似：

private normalizeMcpSuccessResult(
  originalToolName: string,
  args: Record<string, unknown>,
  rawResult: unknown
): ToolResult {
  const mcpResult = rawResult as {
    content?: McpContentItem[];
    isError?: boolean;
  };

  const content = Array.isArray(mcpResult?.content) ? mcpResult.content : [];

  const textContent = content
    .filter((c) => c.type === "text")
    .map((c) => c.text ?? "")
    .filter(Boolean)
    .join("\n");

  const imageItems = content
    .map((item, index) => ({ item, index }))
    .filter(({ item }) => item.type === "image" && typeof item.data === "string");

  const imageRefs = imageItems.map(({ item, index }) => {
    const mimeType = item.mimeType || "image/png";
    return {
      refId: `${originalToolName}:image:${index + 1}`,
      mimeType,
      contentIndex: index,
      sizeChars: item.data?.length ?? 0,
      canHydrateToVisionMessage: true,
    };
  });

  const success = !mcpResult?.isError;

  return {
    success,
    operation: {
      type: inferOperationType(originalToolName),
      target: inferOperationTarget(originalToolName, args),
      targetKind: inferTargetKind(originalToolName, args),
    },
    verification: {
      required: false,
      status: success ? "not_applicable" : "failed",
      method: "mcp_tool_result",
      evidence: {
        textContent,
        imageCount: imageRefs.length,
      },
    },
    data: {
      textContent,
      imageCount: imageRefs.length,
      images: imageRefs,
      resultSummary:
        imageRefs.length > 0
          ? `工具返回了 ${imageRefs.length} 张图片，图片已保存为 artifact，可用于视觉模型分析。`
          : textContent,
    },
    artifactCandidates: imageItems.map(({ item, index }) => {
      const mimeType = item.mimeType || "image/png";

      return {
        kind: "image" as const,
        title: `MCP 图片结果 - ${originalToolName} #${index + 1}`,
        mimeType,
        contentJson: {
          encoding: "base64",
          data: item.data,
          mimeType,
        },
        metadata: {
          source: "mcp",
          toolName: originalToolName,
          contentIndex: index,
          sizeChars: item.data.length,
        },
        defaultRole: "intermediate" as const,
      };
    }),
    outputRefs: [
      ...buildOutputRefs(originalToolName, args, mcpResult),
      ...imageRefs.map((image) => ({
        refId: image.refId,
        role: "result" as const,
      })),
    ],
  };
}

注意：这是短期实现，base64 仍然可能存在 artifactCandidates 里，但后面给 LLM 的 tool message 会剥离它。长期可以改成写文件或对象存储，然后 contentJson 只保存 uri。

四、新增 tool-result-llm-view.ts：构造 LLM 可见工具结果

新增文件：

agent-platform-node/src/runtime/tool-result-llm-view.ts

内容：

import type { ToolResult } from "../tools/tool-types.js";
import { jsonStringify } from "../shared/json.js";

export type LlmToolResult = {
  success: boolean;
  error?: ToolResult["error"];
  operation?: ToolResult["operation"];
  verification?: ToolResult["verification"];
  outputRefs?: ToolResult["outputRefs"];
  data?: unknown;
  artifactCandidates?: unknown[];
};

export function buildLlmToolResult(params: {
  toolResult: ToolResult;
  visualObservation?: unknown;
}): LlmToolResult {
  const { toolResult, visualObservation } = params;

  const safeData = stripLargeFields(toolResult.data);

  return {
    success: toolResult.success,
    error: toolResult.error,
    operation: toolResult.operation,
    verification: toolResult.verification,
    outputRefs: toolResult.outputRefs,
    data: {
      ...(isPlainObject(safeData) ? safeData : { value: safeData }),
      visualObservation: visualObservation ?? undefined,
    },
    artifactCandidates: toolResult.artifactCandidates?.map((item) => ({
      candidateId: item.candidateId,
      kind: item.kind,
      title: item.title,
      mimeType: item.mimeType,
      uri: item.uri,
      metadata: {
        source: item.metadata?.source,
        toolName: item.metadata?.toolName,
        contentIndex: item.metadata?.contentIndex,
        sizeChars: item.metadata?.sizeChars,
        omittedLargeContent: true,
      },
    })),
  };
}

export function buildLlmToolMessageContent(params: {
  toolResult: ToolResult;
  visualObservation?: unknown;
}): string {
  return jsonStringify(buildLlmToolResult(params));
}

function stripLargeFields(value: unknown): unknown {
  return JSON.parse(
    JSON.stringify(value, (_key, val) => {
      if (typeof val === "string" && val.length > 2000) {
        return `[OMITTED_LARGE_STRING:${val.length}_CHARS]`;
      }
      return val;
    })
  );
}

function isPlainObject(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

这个文件的作用：任何给 LLM 的工具结果都必须经过这里，防止 base64 / HTML / 超长字符串进入上下文。

五、新增 vision-observation-builder.ts：图片转视觉摘要

新增文件：

agent-platform-node/src/runtime/vision-observation-builder.ts

内容：

import type { ChatMessage } from "./model-client.js";
import { ModelClient } from "./model-client.js";
import type { ToolArtifactCandidate, ToolResult } from "../tools/tool-types.js";
import { jsonParse } from "../shared/json.js";

export type VisualObservation = {
  source: "vision_model";
  imageCount: number;
  summary: string;
  visibleTexts?: string[];
  uiElements?: Array<{
    type?: string;
    text?: string;
    description?: string;
    location?: string;
  }>;
  state?: Record<string, unknown>;
  nextActionHints?: string[];
  raw?: unknown;
};

export async function buildVisualObservationIfNeeded(params: {
  toolResult: ToolResult;
  modelClient: ModelClient;
  provider: string;
  modelName: string;
  baseUrl?: string | null;
  params?: Record<string, unknown>;
}): Promise<VisualObservation | null> {
  const imageCandidates = findImageCandidates(params.toolResult);

  if (imageCandidates.length === 0) {
    return null;
  }

  const imageMessages = buildImageContentParts(imageCandidates);

  if (imageMessages.length === 0) {
    return null;
  }

  const messages: ChatMessage[] = [
    {
      role: "system",
      content:
        "你是一个视觉观察器。你只负责把图片内容转成用于 Agent 后续工具调用的结构化观察。不要闲聊，不要扩展任务，不要输出 markdown。",
    },
    {
      role: "user",
      content: [
        ...imageMessages,
        {
          type: "text",
          text:
            "请理解这些工具返回的图片。输出严格 JSON，字段包括：summary、visibleTexts、uiElements、state、nextActionHints。重点描述页面状态、关键文本、按钮、输入框、弹窗、错误信息，以及下一步可能操作。",
        },
      ],
    },
  ];

  const result = await params.modelClient.invoke({
    provider: params.provider,
    modelName: params.modelName,
    baseUrl: params.baseUrl,
    params: {
      temperature: 0.2,
      maxTokens: 1200,
      ...(params.params ?? {}),
    },
    messages,
    tools: [],
  });

  const parsed = parseJsonObject(result.content);

  return {
    source: "vision_model",
    imageCount: imageMessages.length,
    summary:
      typeof parsed?.summary === "string"
        ? parsed.summary
        : result.content.slice(0, 1000),
    visibleTexts: Array.isArray(parsed?.visibleTexts)
      ? parsed.visibleTexts.map(String)
      : undefined,
    uiElements: Array.isArray(parsed?.uiElements)
      ? parsed.uiElements
      : undefined,
    state: isPlainObject(parsed?.state) ? parsed.state : undefined,
    nextActionHints: Array.isArray(parsed?.nextActionHints)
      ? parsed.nextActionHints.map(String)
      : undefined,
    raw: parsed ?? result.content,
  };
}

function findImageCandidates(toolResult: ToolResult): ToolArtifactCandidate[] {
  return toolResult.artifactCandidates?.filter((item) => item.kind === "image") ?? [];
}

function buildImageContentParts(candidates: ToolArtifactCandidate[]) {
  const parts: Array<{
    type: "image_url";
    image_url: {
      url: string;
    };
  }> = [];

  for (const candidate of candidates.slice(0, 3)) {
    const contentJson = candidate.contentJson;

    if (!isPlainObject(contentJson)) continue;

    const data = contentJson.data;
    const encoding = contentJson.encoding;
    const mimeType =
      typeof contentJson.mimeType === "string"
        ? contentJson.mimeType
        : candidate.mimeType || "image/png";

    if (encoding === "base64" && typeof data === "string" && data.trim()) {
      parts.push({
        type: "image_url",
        image_url: {
          url: `data:${mimeType};base64,${data}`,
        },
      });
    }
  }

  return parts;
}

function parseJsonObject(text: string): Record<string, unknown> | null {
  const trimmed = text.trim();

  try {
    return JSON.parse(trimmed) as Record<string, unknown>;
  } catch {
    // 尝试提取第一个 JSON 对象
    const start = trimmed.indexOf("{");
    const end = trimmed.lastIndexOf("}");

    if (start >= 0 && end > start) {
      try {
        return JSON.parse(trimmed.slice(start, end + 1)) as Record<string, unknown>;
      } catch {
        return null;
      }
    }

    return null;
  }
}

function isPlainObject(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

注意：

这里最多只传 3 张图片，避免多图爆上下文。
image_url 只在 VisionModel 调用时临时出现。
VisionModel 输出后只保留文本 JSON。
不要把这次 image_url 消息 push 到主 Agent 的长期 messages。
六、修改 agent-runtime.ts：工具执行后自动视觉摘要

在 agent-runtime.ts 顶部新增 import：

import { buildLlmToolMessageContent } from "./tool-result-llm-view.js";
import { buildVisualObservationIfNeeded } from "./vision-observation-builder.js";

找到当前工具执行后这段：

messages.push({
  role: "tool",
  tool_call_id: toolCall.id,
  content: jsonStringify(decoratedToolResult.toolResult),
});

替换成：

const visualObservation = await buildVisualObservationIfNeeded({
  toolResult: decoratedToolResult.toolResult,
  modelClient: this.modelClient,
  provider: args.profile.provider,
  modelName: args.profile.modelName,
  baseUrl: args.profile.baseUrl,
});

if (visualObservation) {
  await this.createStep({
    runId: args.runId,
    stepIndex: stepIndex++,
    stepType: "observation",
    content: visualObservation.summary,
    output: visualObservation,
    metadata: {
      source: "vision_model",
      parentToolCallId: toolCall.id,
      parentToolName: toolCall.name,
    },
  });
}

messages.push({
  role: "tool",
  tool_call_id: toolCall.id,
  content: buildLlmToolMessageContent({
    toolResult: decoratedToolResult.toolResult,
    visualObservation,
  }),
});

这样 LLM 下一轮看到的是：

{
  "success": true,
  "data": {
    "textContent": "",
    "imageCount": 1,
    "images": [
      {
        "refId": "...",
        "mimeType": "image/png",
        "canHydrateToVisionMessage": true
      }
    ],
    "visualObservation": {
      "source": "vision_model",
      "summary": "当前页面是 xxx",
      "visibleTexts": [],
      "uiElements": [],
      "nextActionHints": []
    }
  }
}

没有 base64，但有图片理解结果。

七、不要改 userMessage

不要这样做：

messages.push({
  role: "user",
  content: `视觉观察结果：${JSON.stringify(visualObservation)}`
});

除非临时兼容某些不支持 tool message 的模型。

本系统里推荐把 visualObservation 合并进对应的 role:"tool" 消息，因为它本质是这个工具结果的一部分，应该绑定 tool_call_id。

八、不要修改 rawToolResult 的语义

不要直接把原始 ToolResult 改成视觉摘要。

推荐保留三层：

rawToolResult

用于 tool_end、trace、审计、复盘。里面不能有直接喂给 LLM 的超长 base64，图片用 artifactCandidates 或 uri 表示。

visualObservation

VisionModel 对图片的结构化理解，单独落 observation step。

llmToolResult

喂给 LLM 的轻量结果，等于：

rawToolResult 的轻量视图 + visualObservation
九、验收标准

完成后必须满足：

1. MCP image 不会直接进入 LLM tool message

搜索代码中不应该再有：

content: jsonStringify(decoratedToolResult.toolResult)

应该统一改成：

content: buildLlmToolMessageContent(...)
2. Kimi 多模态消息可以正常构造

ChatMessage.content 支持：

[
  {
    type: "image_url",
    image_url: {
      url: "data:image/png;base64,..."
    }
  },
  {
    type: "text",
    text: "请描述这个图片"
  }
]
3. VisionModel 调用只临时使用 image_url

不要把 image_url/base64 push 到主 Agent 的长期 messages。

4. 主 Agent / 子 Agent 下一轮能看到视觉摘要

下一轮 LLM 能看到类似：

{
  "visualObservation": {
    "summary": "当前页面是...",
    "visibleTexts": ["..."],
    "uiElements": [
      {
        "type": "button",
        "text": "搜索",
        "location": "..."
      }
    ],
    "nextActionHints": ["下一步可以点击搜索按钮"]
  }
}
5. trace 里有 observation step

工具返回图片后，agent_run_steps 里应该出现：

tool_start
tool_end
observation
model_call

其中 observation 的 metadata 包含：

{
  "source": "vision_model",
  "parentToolCallId": "...",
  "parentToolName": "..."
}
6. 大字符串截断

任意喂给 LLM 的 tool message，如果字段字符串超过 2000 字符，需要变成：

[OMITTED_LARGE_STRING:xxxxx_CHARS]
十、最终效果

修改后：

MCP 工具可以返回图片。
图片不会撑爆 LLM 上下文。
Kimi 仍然可以通过 VisionModel 调用理解图片。
图片理解结果会作为结构化 observation 回到工具循环。
子 Agent 能继续正常调用下一步工具。
trace 清晰，可复盘。

---

你让大模型改的时候，重点盯住这句话：

**`role:"tool"` 里放的是 `llmToolResult`，不是原始 `toolResult`；`visualObservation` 合并进这个 `llmToolResult`，不要叠加到原始 userMessage。**