修改任务：补齐 FileSlice 的状态和操作推断
背景

当前 task-envelope-context-hydrator.ts 已经能把 file 类型的 ContextRef 转成 FileSlice，但还有两个问题：

appendRunResultRefs 已经把成功资源状态统一成了：
status: "verified"

但 hydrator 里 file 状态映射只识别：

"write_failed" | "ok" | "unverified"

导致 verified 进入 TaskEnvelope 后会变成：

lastKnownStatus: "unknown"
FileSlice 类型里已经有：
lastKnownOperation?: "read" | "write" | "edit" | "append" | "move" | "delete";

但 hydrator 当前没有从 ref.tags 推断 lastKnownOperation。当前 file 处理逻辑确实只设置了 refId / uri / path / lastKnownStatus / summary。

需要修改的文件
agent-platform-node/src/modules/orchestration/task-envelope-context-hydrator.ts
一、修改 file 状态映射

找到当前代码：

if (ref.kind === "file") {
  files.push({
    refId: ref.refId,
    uri: ref.source?.uri || "",
    path: ref.source?.uri || "",
    lastKnownStatus:
      ref.status === "write_failed"
        ? "failed"
        : ref.status === "ok"
          ? "success"
          : ref.status === "unverified"
            ? "unverified"
            : "unknown",
    summary: ref.summary,
  });
}

改成：

if (ref.kind === "file") {
  files.push({
    refId: ref.refId,
    uri: ref.source?.uri || "",
    path: ref.source?.uri || "",
    lastKnownOperation: inferFileOperation(ref.tags),
    lastKnownStatus: inferFileStatus(ref.status),
    summary: ref.summary,
  });
}
二、新增 inferFileStatus

在文件底部新增：

function inferFileStatus(
  status?: string
): FileSlice["lastKnownStatus"] {
  if (status === "write_failed" || status === "failed") {
    return "failed";
  }

  if (status === "ok" || status === "verified" || status === "success" || status === "ready") {
    return "success";
  }

  if (status === "unverified") {
    return "unverified";
  }

  return "unknown";
}
三、新增 inferFileOperation

在文件底部新增：

function inferFileOperation(
  tags: string[]
): FileSlice["lastKnownOperation"] | undefined {
  const joined = tags.join(" ").toLowerCase();

  if (joined.includes("append")) {
    return "append";
  }

  if (
    joined.includes("write") ||
    joined.includes("wrote") ||
    joined.includes("save") ||
    joined.includes("create")
  ) {
    return "write";
  }

  if (
    joined.includes("edit") ||
    joined.includes("modify") ||
    joined.includes("modified") ||
    joined.includes("patch") ||
    joined.includes("update")
  ) {
    return "edit";
  }

  if (joined.includes("read") || joined.includes("open")) {
    return "read";
  }

  if (joined.includes("move") || joined.includes("rename")) {
    return "move";
  }

  if (joined.includes("delete") || joined.includes("remove")) {
    return "delete";
  }

  return undefined;
}

注意顺序：append 要放在 write 前面，否则 append_write 之类的 tag 会被提前识别成 write。

四、建议顺手优化 url/resource 的 operation 推断

当前 resources 的 lastKnownOperation 只匹配：

["read", "write", "save", "fetch", "crawl", "scrape", "modify", "delete"]

但你在关系里用了：

wrote / modified / deleted

建议抽一个通用函数：

function inferResourceOperation(tags: string[]): string | undefined {
  const joined = tags.join(" ").toLowerCase();

  if (joined.includes("crawl")) return "crawl";
  if (joined.includes("scrape")) return "scrape";
  if (joined.includes("fetch")) return "fetch";
  if (joined.includes("navigate")) return "navigate";
  if (joined.includes("visit")) return "visit";
  if (joined.includes("read")) return "read";
  if (joined.includes("write") || joined.includes("wrote") || joined.includes("save")) return "write";
  if (joined.includes("modify") || joined.includes("modified") || joined.includes("update")) return "modify";
  if (joined.includes("delete") || joined.includes("deleted") || joined.includes("remove")) return "delete";
  if (joined.includes("touched")) return "touched";

  return undefined;
}

然后把 url/resource 这段：

lastKnownOperation: ref.tags.find((t) =>
  ["read", "write", "save", "fetch", "crawl", "scrape", "modify", "delete"].includes(t)
),

改成：

lastKnownOperation: inferResourceOperation(ref.tags),
五、最终期望效果

如果 appendRunResultRefs 产生了：

{
  refId: "file:1.txt",
  kind: "file",
  status: "verified",
  tags: ["file", "write", "verified"]
}

TaskEnvelope 里应该显示成：

## Files
- file:1.txt
  path: 1.txt
  status: success
  lastOperation: write

如果是：

{
  refId: "file:1.txt",
  kind: "file",
  status: "unverified",
  tags: ["file", "write", "unverified"]
}

TaskEnvelope 应该显示：

## Files
- file:1.txt
  path: 1.txt
  status: unverified
  lastOperation: write

如果是写失败：

{
  refId: "file:1.txt",
  kind: "file",
  status: "write_failed",
  tags: ["file", "write_failed"]
}

TaskEnvelope 应该显示：

## Files
- file:1.txt
  path: 1.txt
  status: failed
  lastOperation: write
六、验收标准

测试下面 3 种 ref：

const refs = [
  {
    refId: "file:1.txt",
    kind: "file",
    title: "1.txt",
    summary: "write; verified=true",
    status: "verified",
    source: { uri: "1.txt" },
    tags: ["file", "write", "verified"],
  },
  {
    refId: "file:2.txt",
    kind: "file",
    title: "2.txt",
    summary: "append; verified=true",
    status: "verified",
    source: { uri: "2.txt" },
    tags: ["file", "append", "verified"],
  },
  {
    refId: "file:3.txt",
    kind: "file",
    title: "3.txt",
    summary: "write failed",
    status: "write_failed",
    source: { uri: "3.txt" },
    tags: ["file", "write_failed"],
  },
];

期望 hydrate 后：

files[0].lastKnownStatus === "success";
files[0].lastKnownOperation === "write";

files[1].lastKnownStatus === "success";
files[1].lastKnownOperation === "append";

files[2].lastKnownStatus === "failed";
files[2].lastKnownOperation === "write";
七、不要改的地方

这次只改：

task-envelope-context-hydrator.ts

不要再改：

MainDecision
ExecutionPlan
TaskEnvelopeRenderer
AgentRuntime
appendRunResultRefs

核心一句话：

把 file ref 的 verified/unverified/write_failed 正确转成 TaskEnvelope 能理解的 success/unverified/failed，并把文件操作类型写进 lastKnownOperation