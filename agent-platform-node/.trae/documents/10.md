请修改我的项目：gexulhh520/gexuclaw

目标：
彻底跳过 WorkContext 业务链路。
以后新任务不再插入 work_contexts 表。
主 Agent 不再判断 targetWorkContextUid / createWorkContext。
上下文直接基于 Session 构建 refs，然后交给主 Agent。
AgentRun 直接挂 sessionId。
AgentArtifact 直接挂 sessionId / runId，不再强依赖 workContextId。

当前问题：
现在 WorkContext 参与了太多流程：
1. orchestration.service.ts 里 LLM 决策成功后才 createWorkContext。
2. SessionRuntimeSnapshotBuilder 会读取当前 session 下多个 WorkContext。
3. SessionContextIndexBuilder 会把 WorkContext 转成 ref。
4. MainDecision schema 要求 targetWorkContextUid / createWorkContext。
5. agentArtifacts.workContextId 当前是 notNull，导致 artifact 必须绑定 WorkContext。
6. ArtifactCoordinator 在没有 workContextUid 时直接 return，不保存产物。
7. 前端 API 和 workbench 也围绕 WorkContext 展示。

现在要改成：
Session 是唯一上下文容器。
Run 是执行记录。
Artifact 直接挂 Session。
Refs 从 Session 下的 runs / steps / artifacts / files / agents 构建。
用户说“继续”时，从 sessions.metadataJson 或最近 failed run 恢复原始任务。

============================================================
一、数据库 schema 修改
============================================================

文件：
agent-platform-node/src/db/schema.ts

当前 agentArtifacts 有：

```ts
workContextId: integer("work_context_id").notNull(),

必须改成可空，并增加 sessionId：

sessionId: text("session_id"),
workContextId: integer("work_context_id"),

推荐改成：

export const agentArtifacts = pgTable("agent_artifacts", {
  id: serial("id").primaryKey(),
  artifactUid: text("artifact_uid").notNull().unique(),

  // 新增：Artifact 直接归属 Session
  sessionId: text("session_id"),

  // 保留旧字段兼容历史数据，但新逻辑不再写入
  workContextId: integer("work_context_id"),

  runId: integer("run_id"),
  artifactType: text("artifact_type").notNull(),
  artifactRole: text("artifact_role").notNull().default("output"),
  title: text("title").notNull(),
  mimeType: text("mime_type"),
  contentText: text("content_text").notNull().default(""),
  contentJson: text("content_json").notNull().default("{}"),
  uri: text("uri"),
  status: text("status").notNull().default("ready"),
  sourceRunId: integer("source_run_id"),
  sourceArtifactIdsJson: text("source_artifact_ids_json").notNull().default("[]"),
  metadataJson: text("metadata_json").notNull().default("{}"),
  createdAt: text("created_at").notNull(),
  updatedAt: text("updated_at").notNull(),
});

agentRuns 里的 workContextId 可以暂时保留，但新代码不再写：

workContextId: text("work_context_id"),

sessions.metadataJson 要承担原来 WorkContext projection 的一部分状态，例如：

{
  "currentStage": "planning | executing | blocked | completed | waiting_user",
  "lastEffectiveUserMessage": "打开百度浏览器",
  "lastRecoverableRunUid": "run_xxx",
  "lastFailedRunUid": "run_xxx",
  "lastSuccessfulRunUid": "run_xxx",
  "recoverable": true,
  "recentRefs": ["run:run_xxx", "artifact:artifact_xxx"],
  "openIssues": []
}

需要新增数据库迁移 SQL：

ALTER TABLE agent_artifacts ADD COLUMN IF NOT EXISTS session_id text;

ALTER TABLE agent_artifacts ALTER COLUMN work_context_id DROP NOT NULL;

UPDATE agent_artifacts a
SET session_id = wc.session_id
FROM work_contexts wc
WHERE a.work_context_id = wc.id
  AND a.session_id IS NULL;

CREATE INDEX IF NOT EXISTS idx_agent_artifacts_session_id
ON agent_artifacts(session_id);

CREATE INDEX IF NOT EXISTS idx_agent_artifacts_run_id
ON agent_artifacts(run_id);

work_contexts 表暂时不要物理删除，先废弃不用。
避免旧数据和旧接口立刻崩。

============================================================
二、修改 Orchestration Schema

文件：
agent-platform-node/src/modules/orchestration/orchestration.schema.ts

当前 chatRequestSchema 还有：

workContextId: z.string().optional()

改成：

export const chatRequestSchema = z.object({
  sessionId: z.string(),
  message: z.string().min(1, "消息不能为空"),
  selectedAgentId: z.string().optional(),
});

当前 chatResponseSchema 还有：

workContextId: z.string()

改成：

export const chatResponseSchema = z.object({
  message: z.string(),
  runId: z.string().optional(),
  agentId: z.string().optional(),
  artifacts: z.array(z.object({
    id: z.string(),
    type: z.string(),
    title: z.string(),
  })).optional(),
});

删除或废弃 DelegateEnvelope 里的 workContextId：

workContextId: z.string()

改成：

sessionId: z.string()

或者如果当前没用 DelegateEnvelope，可以暂时保留类型但不要在新流程使用。

============================================================
三、修改 MainDecision Schema

文件：
agent-platform-node/src/modules/orchestration/main-decision.schema.ts

当前包含：

"create_work_context",
"use_existing_work_context",
"switch_work_context",
targetWorkContextUid,
createWorkContext,
ambiguity.candidateWorkContextUids

全部删除。

改成：

import { z } from "zod";

export const planStepDraftSchema = z.object({
  targetAgentUid: z.string().min(1),
  objective: z.string().min(1),
  inputRefIds: z.array(z.string()).default([]),
  expectedResultKind: z.enum([
    "answer",
    "artifact",
    "file_change",
    "diagnosis",
    "verification",
  ]),
  requireVerification: z.boolean(),
});

export const mainDecisionSchema = z.object({
  decisionType: z.enum([
    "answer_directly",
    "delegate",
    "multi_step_plan",
    "ask_user",
    "explain_trace",
    "verify_execution",
    "recover_execution",
  ]),

  primaryRefs: z.array(z.string()).default([]),
  secondaryRefs: z.array(z.string()).default([]),

  targetAgentUid: z.string().nullable().default(null),

  plan: z
    .object({
      steps: z.array(planStepDraftSchema).default([]),
    })
    .nullable()
    .default(null),

  response: z.string().nullable().optional().default(null),

  ambiguity: z
    .object({
      candidateRefIds: z.array(z.string()).default([]),
      question: z.string().min(1),
    })
    .nullable()
    .optional()
    .default(null),

  confidence: z.enum(["high", "medium", "low"]),
  reasoning: z.string().min(1),
});

export type MainDecision = z.infer<typeof mainDecisionSchema>;
============================================================
四、修改 Orchestration Types

文件：
agent-platform-node/src/modules/orchestration/orchestration.types.ts

ContextRefKind 删除：
"work_context"

保留：

export type ContextRefKind =
  | "session"
  | "run"
  | "step"
  | "artifact"
  | "file"
  | "url"
  | "resource"
  | "agent"
  | "tool";
ContextRef 删除 workContextUid，增加 sessionId：
export type ContextRef = {
  refId: string;
  kind: ContextRefKind;
  title: string;
  summary: string;

  sessionId?: string;
  status?: string;

  source: {
    table?: "sessions" | "agent_runs" | "agent_run_steps" | "agent_artifacts";
    uid?: string;
    runUid?: string;
    stepIndex?: number;
    uri?: string;
  };

  tags: string[];

  evidence?: {
    selectedInUI?: boolean;
    recencyRank?: number;
    statusSignals?: string[];
    semanticSignals?: string[];
  };

  updatedAt?: string;
};
RuntimeRunTrace 删除 workContextUid：
export type RuntimeRunTrace = {
  runUid: string;
  agentUid: string;
  agentName: string;
  mode: string;
  status: string;

  sessionId?: string;
  parentRunId?: number;

  userMessage: string;
  resultSummary?: string;
  errorMessage?: string;

  steps: RuntimeStepTrace[];
  createdAt: string;
};
删除 WorkContextCard / WorkContextDecisionCard。
SessionRuntimeSnapshot 改成：
export type SessionRuntimeSnapshot = {
  userMessage: string;
  effectiveUserMessage: string;

  session: {
    sessionUid: string;
    title?: string;
    description?: string;
    metadata?: Record<string, unknown>;
  };

  sessionState: {
    currentStage?: string;
    recoverable?: boolean;
    lastEffectiveUserMessage?: string;
    lastRecoverableRunUid?: string;
    lastFailedRunUid?: string;
    lastSuccessfulRunUid?: string;
    recentRefs?: string[];
    openIssues?: Array<{
      refId?: string;
      summary: string;
      severity?: "low" | "medium" | "high";
      status: "open" | "resolved";
    }>;
  };

  globalRecentRuns: RuntimeRunTrace[];

  globalRecentArtifacts: Array<{
    artifactUid: string;
    sessionId?: string;
    title: string;
    artifactType: string;
    artifactRole?: string;
    summary?: string;
    createdAt?: string;
  }>;

  availableAgents: Array<{
    agentUid: string;
    name: string;
    description?: string;
    capabilities?: string[];
    status?: string;
  }>;
};
MainDecisionInput 改成：
export type MainDecisionInput = {
  userMessage: string;
  effectiveUserMessage: string;
  sessionState: SessionRuntimeSnapshot["sessionState"];

  refs: ContextRef[];
  relations: ContextRelation[];

  availableAgents: AgentDecisionCard[];
};
MainDecision 删除：
targetWorkContextUid
createWorkContext
ambiguity.candidateWorkContextUids
ExecutionPlan 删除 workContextUid，增加 sessionId：
export type ExecutionPlan = {
  planUid: string;

  mode:
    | "direct_response"
    | "single_agent"
    | "sequential_agents"
    | "parallel_agents";

  sessionId: string;

  selectedRefs: string[];

  steps: Array<{
    stepUid: string;
    targetAgentUid: string;
    objective: string;
    inputRefIds: string[];
    dependsOn: string[];
    expectedResultKind: "answer" | "artifact" | "file_change" | "diagnosis" | "verification";
    requireVerification: boolean;
    allowedTools: string[];
  }>;

  finalResponseStrategy:
    | "use_direct_response"
    | "compose_from_agent_results"
    | "compose_from_ledger";
};
============================================================
五、修改 SessionRuntimeSnapshotBuilder

文件：
agent-platform-node/src/modules/orchestration/session-runtime-snapshot-builder.ts

当前会读取 workContexts。这个文件现在必须完全移除 WorkContext 查询。它目前会读取当前 session 下多个 WorkContext、recent runs、artifacts、agents。

改成只读取：

session
session.metadataJson
globalRecentRuns
globalRecentArtifacts
availableAgents

删除：

import workContexts
getWorkContexts
buildTopRefsFromProjection
selectedWorkContextUid 参数
workContexts 字段

改后结构：

import { desc, eq, inArray } from "drizzle-orm";
import { db } from "../../db/client.js";
import { sessions, agentArtifacts, agents, agentRuns } from "../../db/schema.js";
import { jsonParse } from "../../shared/json.js";
import { LedgerReader } from "./ledger-reader.js";
import type { SessionRuntimeSnapshot } from "./orchestration.types.js";

export class SessionRuntimeSnapshotBuilder {
  private ledgerReader = new LedgerReader();

  async build(input: {
    sessionId: string;
    userMessage: string;
    effectiveUserMessage: string;
  }): Promise<SessionRuntimeSnapshot> {
    const { sessionId, userMessage, effectiveUserMessage } = input;

    const session = await this.getSession(sessionId);
    const sessionMetadata = jsonParse<Record<string, unknown>>(
      session?.metadataJson,
      {}
    );

    const globalRecentRuns = await this.ledgerReader.getRecentRunsWithSteps({
      sessionId,
      limit: 10,
      stepsPerRun: 20,
    });

    const globalRecentArtifacts = await this.getRecentArtifacts(sessionId);
    const availableAgents = await this.getAvailableAgents(session);

    return {
      userMessage,
      effectiveUserMessage,
      session: {
        sessionUid: sessionId,
        title: session?.title,
        description: session?.description,
        metadata: sessionMetadata,
      },
      sessionState: {
        currentStage: sessionMetadata.currentStage as string | undefined,
        recoverable: sessionMetadata.recoverable as boolean | undefined,
        lastEffectiveUserMessage: sessionMetadata.lastEffectiveUserMessage as string | undefined,
        lastRecoverableRunUid: sessionMetadata.lastRecoverableRunUid as string | undefined,
        lastFailedRunUid: sessionMetadata.lastFailedRunUid as string | undefined,
        lastSuccessfulRunUid: sessionMetadata.lastSuccessfulRunUid as string | undefined,
        recentRefs: Array.isArray(sessionMetadata.recentRefs)
          ? (sessionMetadata.recentRefs as string[])
          : [],
        openIssues: Array.isArray(sessionMetadata.openIssues)
          ? (sessionMetadata.openIssues as any)
          : [],
      },
      globalRecentRuns,
      globalRecentArtifacts,
      availableAgents,
    };
  }

  private async getSession(sessionId: string) {
    const [session] = await db
      .select({
        title: sessions.title,
        description: sessions.description,
        agentIdsJson: sessions.agentIdsJson,
        metadataJson: sessions.metadataJson,
      })
      .from(sessions)
      .where(eq(sessions.sessionUid, sessionId))
      .limit(1);

    return session;
  }

  private async getRecentArtifacts(sessionId: string) {
    const artifacts = await db
      .select({
        artifactUid: agentArtifacts.artifactUid,
        sessionId: agentArtifacts.sessionId,
        title: agentArtifacts.title,
        artifactType: agentArtifacts.artifactType,
        artifactRole: agentArtifacts.artifactRole,
        contentText: agentArtifacts.contentText,
        createdAt: agentArtifacts.createdAt,
      })
      .from(agentArtifacts)
      .where(eq(agentArtifacts.sessionId, sessionId))
      .orderBy(desc(agentArtifacts.id))
      .limit(20);

    return artifacts.map((art) => ({
      artifactUid: art.artifactUid,
      sessionId: art.sessionId ?? undefined,
      title: art.title,
      artifactType: art.artifactType,
      artifactRole: art.artifactRole ?? undefined,
      summary: art.contentText?.slice(0, 200),
      createdAt: art.createdAt,
    }));
  }

  private async getAvailableAgents(session: { agentIdsJson: string } | undefined) {
    const sessionAgentUids = session ? jsonParse<string[]>(session.agentIdsJson, []) : [];

    if (sessionAgentUids.length === 0) {
      return [];
    }

    const activeAgents = await db
      .select()
      .from(agents)
      .where(inArray(agents.agentUid, sessionAgentUids));

    return activeAgents.map((agent) => {
      const capabilities = jsonParse<string[]>(agent.capabilitiesJson, []);
      return {
        agentUid: agent.agentUid,
        name: agent.name,
        description: agent.description || undefined,
        capabilities,
        status: agent.status,
      };
    });
  }
}
============================================================
六、修改 SessionContextIndexBuilder

文件：
agent-platform-node/src/modules/orchestration/session-context-index-builder.ts

当前会把 WorkContext / Run / Step / Artifact / Agent / File 转成 refs，并且第一步就是 WorkContext refs。

删除：

WorkContext refs 构建
buildWorkContextStatusSignals
run belongs_to work_context relation
step belongs_to work_context relation
artifact belongs_to work_context relation
所有 workContextUid 字段

新增：

session ref
sessionState refs
run belongs_to session relation
artifact belongs_to session relation

替换成：

import type {
  SessionRuntimeSnapshot,
  SessionContextIndex,
  ContextRef,
  ContextRelation,
} from "./orchestration.types.js";

export class SessionContextIndexBuilder {
  build(snapshot: SessionRuntimeSnapshot): SessionContextIndex {
    const refs: ContextRef[] = [];
    const relations: ContextRelation[] = [];

    const sessionRefId = `session:${snapshot.session.sessionUid}`;

    refs.push({
      refId: sessionRefId,
      kind: "session",
      title: snapshot.session.title || snapshot.session.sessionUid,
      summary: snapshot.session.description || "",
      sessionId: snapshot.session.sessionUid,
      status: snapshot.sessionState.currentStage || "active",
      source: { table: "sessions", uid: snapshot.session.sessionUid },
      tags: [
        "session",
        snapshot.sessionState.currentStage || "active",
        snapshot.sessionState.recoverable ? "recoverable" : "",
      ].filter(Boolean),
      evidence: {
        statusSignals: [
          snapshot.sessionState.lastFailedRunUid ? "failed_run" : "",
          snapshot.sessionState.recoverable ? "recoverable" : "",
        ].filter(Boolean),
      },
    });

    for (const run of snapshot.globalRecentRuns) {
      const runRefId = `run:${run.runUid}`;

      refs.push({
        refId: runRefId,
        kind: "run",
        title: `${run.agentName} run`,
        summary: run.resultSummary || run.errorMessage || run.userMessage.slice(0, 100),
        sessionId: run.sessionId,
        status: run.status,
        source: { table: "agent_runs", uid: run.runUid, runUid: run.runUid },
        tags: [
          "run",
          run.status,
          run.mode,
          snapshot.sessionState.lastRecoverableRunUid === run.runUid ? "recoverable" : "",
          snapshot.sessionState.lastFailedRunUid === run.runUid ? "last_failed" : "",
        ].filter(Boolean),
        evidence: {
          statusSignals: run.status === "failed" ? ["failed"] : [],
        },
        updatedAt: run.createdAt,
      });

      relations.push({
        fromRefId: runRefId,
        toRefId: sessionRefId,
        relation: "belongs_to",
      });

      relations.push({
        fromRefId: runRefId,
        toRefId: `agent:${run.agentUid}`,
        relation: "executed_by",
      });

      for (const step of run.steps) {
        const stepRefId = `step:${run.runUid}:${step.stepIndex}`;
        const isFailed = step.toolStatus === "failed" || step.toolStatus === "error";
        const isToolWrite = step.toolName && this.isWriteTool(step.toolName);

        refs.push({
          refId: stepRefId,
          kind: "step",
          title: step.toolName
            ? `${step.toolName} ${isFailed ? "失败" : step.stepType}`
            : `${step.stepType}`,
          summary: this.buildStepSummary(step),
          sessionId: run.sessionId,
          status: step.toolStatus || step.stepType,
          source: {
            table: "agent_run_steps",
            uid: `${run.runUid}:${step.stepIndex}`,
            runUid: run.runUid,
            stepIndex: step.stepIndex,
          },
          tags: ["step", step.stepType, step.toolName || "", isFailed ? "failed" : ""].filter(Boolean),
          evidence: {
            statusSignals: isFailed ? ["failed", "tool_error"] : [],
            semanticSignals: isToolWrite ? ["写入", "文件"] : [],
          },
          updatedAt: step.createdAt,
        });

        relations.push({
          fromRefId: stepRefId,
          toRefId: runRefId,
          relation: "belongs_to",
        });

        if (isToolWrite && step.metadataJson) {
          const metadata = step.metadataJson as Record<string, unknown>;
          const targetPath = metadata.targetPath || metadata.path;

          if (typeof targetPath === "string") {
            const fileRefId = `file:${targetPath}`;

            if (!refs.find((r) => r.refId === fileRefId)) {
              refs.push({
                refId: fileRefId,
                kind: "file",
                title: targetPath.split("/").pop() || targetPath,
                summary: `目标文件${isFailed ? "，最近写入失败" : ""}`,
                sessionId: run.sessionId,
                status: isFailed ? "write_failed" : "ok",
                source: { table: "agent_run_steps", uri: targetPath },
                tags: ["file", isFailed ? "write_failed" : ""].filter(Boolean),
              });
            }

            relations.push({
              fromRefId: stepRefId,
              toRefId: fileRefId,
              relation: "attempted_write",
            });
          }
        }
      }
    }

    for (const art of snapshot.globalRecentArtifacts) {
      const refId = `artifact:${art.artifactUid}`;

      refs.push({
        refId,
        kind: "artifact",
        title: art.title,
        summary: art.summary || `${art.artifactType} artifact`,
        sessionId: art.sessionId,
        status: art.artifactRole === "pending_write" ? "pending_write" : "ready",
        source: { table: "agent_artifacts", uid: art.artifactUid },
        tags: ["artifact", art.artifactType, art.artifactRole || ""].filter(Boolean),
        updatedAt: art.createdAt,
      });

      relations.push({
        fromRefId: refId,
        toRefId: sessionRefId,
        relation: "belongs_to",
      });
    }

    for (const agent of snapshot.availableAgents) {
      const refId = `agent:${agent.agentUid}`;
      if (!refs.find((r) => r.refId === refId)) {
        refs.push({
          refId,
          kind: "agent",
          title: agent.name,
          summary: agent.description || `${agent.name} Agent`,
          status: agent.status,
          source: { table: "sessions", uid: agent.agentUid },
          tags: ["agent", ...(agent.capabilities || [])],
        });
      }
    }

    console.log(`[ContextIndexBuilder] Refs数量: ${refs.length}, Relations数量: ${relations.length}`);

    return { refs, relations };
  }

  private buildStepSummary(step: SessionRuntimeSnapshot["globalRecentRuns"][0]["steps"][0]): string {
    if (step.toolName) {
      const status = step.toolStatus || step.stepType;
      return `${step.toolName} 调用，状态: ${status}`;
    }
    return `${step.stepType}`;
  }

  private isWriteTool(toolName: string): boolean {
    const writeTools = ["fs_write", "fs_append", "fs_edit", "fs_apply_patch"];
    return writeTools.some((w) => toolName.includes(w));
  }
}
============================================================
七、修改 LedgerReader

文件：
agent-platform-node/src/modules/orchestration/ledger-reader.ts

当前会把 run.workContextId 映射到 workContextUid。

删除：

workContextUid: run.workContextId ?? undefined,

两处都删：

getRecentRunsWithSteps
getRunWithSteps

RuntimeRunTrace 类型也已经删除 workContextUid。

============================================================
八、修改 DecisionContractValidator

文件：
agent-platform-node/src/modules/orchestration/decision-contract-validator.ts

删除所有 WorkContext 校验：

validWorkContextUids
targetWorkContextUid 校验
createWorkContext 校验
mayCreateWorkContext
create_work_context 特殊规则
fallbackDecision.ambiguity.candidateWorkContextUids

新的 validator 只校验：

primaryRefs / secondaryRefs 是否存在
targetAgentUid 是否存在
plan.steps[].targetAgentUid 是否存在
plan.steps[].inputRefIds 是否存在
decisionType 必填字段

替换核心逻辑：

const validRefIds = new Set(contextIndex.refs.map((ref) => ref.refId));
const validAgentUids = new Set(snapshot.availableAgents.map((a) => a.agentUid));

for (const refId of decision.primaryRefs) {
  if (!validRefIds.has(refId)) {
    issues.push(`primaryRef ${refId} 不存在`);
  }
}

for (const refId of decision.secondaryRefs) {
  if (!validRefIds.has(refId)) {
    issues.push(`secondaryRef ${refId} 不存在`);
  }
}

if (decision.targetAgentUid && !validAgentUids.has(decision.targetAgentUid)) {
  issues.push(`targetAgentUid ${decision.targetAgentUid} 不存在`);
}

if (decision.plan?.steps) {
  for (const step of decision.plan.steps) {
    if (!validAgentUids.has(step.targetAgentUid)) {
      issues.push(`plan step targetAgentUid ${step.targetAgentUid} 不存在`);
    }

    for (const refId of step.inputRefIds) {
      if (!validRefIds.has(refId)) {
        issues.push(`plan step inputRefId ${refId} 不存在`);
      }
    }
  }
}

const requiresPlan = [
  "delegate",
  "multi_step_plan",
  "recover_execution",
  "verify_execution",
].includes(decision.decisionType);

if (requiresPlan && (!decision.plan || decision.plan.steps.length === 0)) {
  issues.push(`${decision.decisionType} 需要 plan.steps`);
}

if (decision.decisionType === "answer_directly" && !decision.response) {
  issues.push("answer_directly 需要 response");
}

if (decision.decisionType === "ask_user" && (!decision.ambiguity || !decision.ambiguity.question)) {
  issues.push("ask_user 需要 ambiguity.question");
}

if (issues.length > 0) {
  return {
    valid: false,
    fallbackDecision: {
      decisionType: "ask_user",
      primaryRefs: [],
      secondaryRefs: [],
      targetAgentUid: null,
      plan: null,
      response: null,
      ambiguity: {
        candidateRefIds: decision.primaryRefs.filter((refId) => validRefIds.has(refId)),
        question: `我发现了一些问题：${issues.join("；")}。请确认下一步。`,
      },
      confidence: "low",
      reasoning: `Decision validation failed: ${issues.join("; ")}`,
    },
    issues,
  };
}
============================================================
九、修改 ExecutionPlanCompiler

文件：
agent-platform-node/src/modules/orchestration/execution-plan-compiler.ts

当前 compile 会把 decision.targetWorkContextUid 写到 plan.workContextUid，并支持 create_work_context。

改动：

compile input 增加 sessionId 或从 snapshot.session.sessionUid 取。
删除 workContextUid。
删除 create_work_context 映射。

修改返回：

return {
  planUid,
  mode,
  sessionId: input.snapshot.session.sessionUid,
  selectedRefs,
  steps,
  finalResponseStrategy,
};

mapDecisionTypeToMode 改成：

private mapDecisionTypeToMode(
  decisionType: MainDecision["decisionType"],
  hasSteps: boolean
): ExecutionPlan["mode"] {
  switch (decisionType) {
    case "answer_directly":
      return "direct_response";
    case "delegate":
      return "single_agent";
    case "multi_step_plan":
    case "recover_execution":
    case "verify_execution":
      return "sequential_agents";
    default:
      return "direct_response";
  }
}
============================================================
十、修改 MainAgent Prompt 和 Input

文件：
agent-platform-node/src/modules/orchestration/main-agent.ts

当前 prompt 明确要求：

targetWorkContextUid 必须来自 workContexts
createWorkContext
create_work_context
switch_work_context
use_existing_work_context

全部删除。当前 MainAgent 的输入会包含 workContexts 和 selectedWorkContextUid。

buildMainDecisionInput 改成：

private buildMainDecisionInput(input: {
  userMessage: string;
  snapshot: SessionRuntimeSnapshot;
  contextIndex: SessionContextIndex;
}): MainDecisionInput {
  return {
    userMessage: input.userMessage,
    effectiveUserMessage: input.snapshot.effectiveUserMessage,
    sessionState: input.snapshot.sessionState,
    refs: this.truncateRefsForDecision(input.contextIndex.refs),
    relations: input.contextIndex.relations,
    availableAgents: input.snapshot.availableAgents.map((agent) => ({
      agentUid: agent.agentUid,
      name: agent.name,
      description: this.truncateText(agent.description, 200),
      capabilities: agent.capabilities,
    })),
  };
}

getMainDecisionJsonContract 改成：

{
  "decisionType": "answer_directly | delegate | multi_step_plan | ask_user | explain_trace | verify_execution | recover_execution",
  "primaryRefs": ["string"],
  "secondaryRefs": ["string"],
  "targetAgentUid": "string or null",
  "plan": {
    "steps": [
      {
        "targetAgentUid": "string",
        "objective": "string",
        "inputRefIds": ["string"],
        "expectedResultKind": "answer | artifact | file_change | diagnosis | verification",
        "requireVerification": true
      }
    ]
  } 或 null,
  "response": "string or null",
  "ambiguity": {
    "candidateRefIds": ["string"],
    "question": "string"
  } 或 null,
  "confidence": "high | medium | low",
  "reasoning": "string"
}

Prompt 规则改成：

你是主 Agent 的结构化决策器，不是执行器。

你会收到：
1. userMessage：用户本轮原始消息
2. effectiveUserMessage：系统恢复后的真实任务消息；如果用户说“继续”，这里可能是上一轮失败任务
3. sessionState：当前会话状态
4. ContextRefs
5. ContextRelations
6. availableAgents

你不能创建、选择、切换 WorkContext。
系统已经取消 WorkContext 概念。
你只需要基于 Session refs 判断：
- 是否直接回复
- 是否委派
- 是否多步骤计划
- 是否恢复失败任务
- 是否验证执行结果
- 是否需要追问

规则：
1. primaryRefs、secondaryRefs、plan.steps[].inputRefIds 必须来自 refs[].refId。
2. targetAgentUid、plan.steps[].targetAgentUid 必须来自 availableAgents[].agentUid。
3. 如果 userMessage 是“继续/重试/接着”，以 effectiveUserMessage 和 sessionState.lastRecoverableRunUid 为准。
4. 如果 sessionState.currentStage=blocked 或 recoverable=true，优先考虑 recover_execution。
5. 如果用户表达执行失败、报错、没生效、没写入，优先关注 failed/recoverable/write_failed refs。
6. 如果任务是新任务，直接 delegate 或 multi_step_plan，不要创建 WorkContext。
7. answer_directly 必须填写 response。
8. ask_user 必须填写 ambiguity.question。
9. delegate、recover_execution、verify_execution、multi_step_plan 必须填写 plan.steps。
10. reasoning 不超过 300 字。

repair fallback 也要删除 WorkContext 字段。

============================================================
十一、修改 OrchestrationService 主流程

文件：
agent-platform-node/src/modules/orchestration/orchestration.service.ts

当前这个文件里有：

createWorkContext
finalWorkContextId
selectedWorkContextUid
updateWorkContextProjection
updateWorkContextRunBinding
runtime.run 传 workContextId
executePlanAsync 依赖 plan.workContextUid

这些全部要改。

删除 imports：
workContexts
updateWorkContextRunBinding
updateWorkContextProjection

如果 schema 还要用 workContexts 做历史兼容，orchestration.service 不要再引入。

删除本文件里的 createWorkContext 函数。
handleChat 删除 workContextId：
const { sessionId, message, selectedAgentId } = input;

返回删除 workContextId：

return {
  message: "正在处理您的请求...",
  runId: run.runUid,
  agentId: "main_agent",
};
processChatAsync 改成 Session 状态驱动。

新增辅助函数：

function isContinuationMessage(message: string): boolean {
  const text = message.trim();
  return /^(继续|继续执行|接着|接着执行|重试|再试一次|重新试试|再来一次|刚才那个|上次那个|继续刚才|继续上次|恢复执行)$/.test(text);
}

新增读取 session metadata：

async function getSessionMetadata(sessionId: string) {
  const [session] = await db
    .select({
      metadataJson: sessions.metadataJson,
    })
    .from(sessions)
    .where(eq(sessions.sessionUid, sessionId))
    .limit(1);

  return jsonParse<Record<string, any>>(session?.metadataJson, {});
}

新增更新 session metadata：

async function patchSessionMetadata(
  sessionId: string,
  patch: Record<string, unknown>
) {
  const [session] = await db
    .select({ metadataJson: sessions.metadataJson })
    .from(sessions)
    .where(eq(sessions.sessionUid, sessionId))
    .limit(1);

  const metadata = jsonParse<Record<string, unknown>>(session?.metadataJson, {});
  const next = {
    ...metadata,
    ...patch,
  };

  await db
    .update(sessions)
    .set({
      metadataJson: jsonStringify(next),
      updatedAt: nowIso(),
    })
    .where(eq(sessions.sessionUid, sessionId));

  return next;
}

新增从最近失败 run 恢复：

async function findLatestRecoverableRun(sessionId: string) {
  const metadata = await getSessionMetadata(sessionId);

  if (metadata.lastRecoverableRunUid) {
    const [run] = await db
      .select()
      .from(agentRuns)
      .where(eq(agentRuns.runUid, String(metadata.lastRecoverableRunUid)))
      .limit(1);

    if (run) return run;
  }

  const [latestFailedRun] = await db
    .select()
    .from(agentRuns)
    .where(eq(agentRuns.sessionId, sessionId))
    .orderBy(desc(agentRuns.id))
    .limit(10);

  return latestFailedRun;
}

更严格一点可以查 status=failed，但如果当前没有 and 条件导入，就先从最近 runs 里筛：

async function findLatestRecoverableRun(sessionId: string) {
  const metadata = await getSessionMetadata(sessionId);

  if (metadata.lastRecoverableRunUid) {
    const [run] = await db
      .select()
      .from(agentRuns)
      .where(eq(agentRuns.runUid, String(metadata.lastRecoverableRunUid)))
      .limit(1);

    if (run) return run;
  }

  const runs = await db
    .select()
    .from(agentRuns)
    .where(eq(agentRuns.sessionId, sessionId))
    .orderBy(desc(agentRuns.id))
    .limit(20);

  return runs.find((run) => run.status === "failed" && !!run.userMessage);
}

新增 effective message：

async function resolveEffectiveUserMessage(input: {
  sessionId: string;
  message: string;
}) {
  if (!isContinuationMessage(input.message)) {
    return input.message;
  }

  const metadata = await getSessionMetadata(input.sessionId);

  if (typeof metadata.lastEffectiveUserMessage === "string" && metadata.lastEffectiveUserMessage.trim()) {
    return metadata.lastEffectiveUserMessage;
  }

  const latestRecoverableRun = await findLatestRecoverableRun(input.sessionId);

  return latestRecoverableRun?.userMessage || input.message;
}

新增运行开始更新：

async function markSessionPlanning(input: {
  sessionId: string;
  runUid: string;
  effectiveUserMessage: string;
}) {
  const metadata = await getSessionMetadata(input.sessionId);
  const recentRefs = Array.isArray(metadata.recentRefs) ? metadata.recentRefs : [];

  await patchSessionMetadata(input.sessionId, {
    currentStage: "planning",
    recoverable: true,
    lastEffectiveUserMessage: input.effectiveUserMessage,
    lastRecoverableRunUid: input.runUid,
    recentRefs: Array.from(new Set([`run:${input.runUid}`, ...recentRefs])).slice(0, 20),
  });
}

新增失败更新：

async function markSessionBlocked(input: {
  sessionId: string;
  runUid: string;
  effectiveUserMessage: string;
  error: unknown;
}) {
  const metadata = await getSessionMetadata(input.sessionId);
  const recentRefs = Array.isArray(metadata.recentRefs) ? metadata.recentRefs : [];
  const openIssues = Array.isArray(metadata.openIssues) ? metadata.openIssues : [];

  const errorMessage =
    input.error instanceof Error ? input.error.message : String(input.error || "未知错误");

  await patchSessionMetadata(input.sessionId, {
    currentStage: "blocked",
    recoverable: true,
    lastEffectiveUserMessage: input.effectiveUserMessage,
    lastRecoverableRunUid: input.runUid,
    lastFailedRunUid: input.runUid,
    lastError: errorMessage,
    lastErrorAt: nowIso(),
    recentRefs: Array.from(new Set([`run:${input.runUid}`, ...recentRefs])).slice(0, 20),
    openIssues: [
      {
        refId: `run:${input.runUid}`,
        summary: `执行失败：${errorMessage}`,
        severity: "high",
        status: "open",
      },
      ...openIssues,
    ].slice(0, 10),
  });

  await db
    .update(agentRuns)
    .set({
      errorMessage,
      updatedAt: nowIso(),
    })
    .where(eq(agentRuns.runUid, input.runUid));
}

新增成功更新：

async function markSessionCompleted(input: {
  sessionId: string;
  runUid: string;
  effectiveUserMessage: string;
  summary: string;
}) {
  const metadata = await getSessionMetadata(input.sessionId);
  const recentRefs = Array.isArray(metadata.recentRefs) ? metadata.recentRefs : [];
  const openIssues = Array.isArray(metadata.openIssues) ? metadata.openIssues : [];

  await patchSessionMetadata(input.sessionId, {
    currentStage: "completed",
    recoverable: false,
    lastEffectiveUserMessage: input.effectiveUserMessage,
    lastSuccessfulRunUid: input.runUid,
    lastRecoverableRunUid: null,
    lastError: null,
    recentRefs: Array.from(new Set([`run:${input.runUid}`, ...recentRefs])).slice(0, 20),
    openIssues: openIssues.map((issue: any) => ({
      ...issue,
      status: "resolved",
    })),
    progressSummary: input.summary,
  });
}

processChatAsync 替换为 Session 版：

async function processChatAsync(input: ChatRequestInput, mainRunId: string): Promise<void> {
  const { sessionId, message } = input;

  let effectiveUserMessage = message;

  try {
    console.log("[processChatAsync] Session-only orchestration flow...");

    effectiveUserMessage = await resolveEffectiveUserMessage({
      sessionId,
      message,
    });

    await markSessionPlanning({
      sessionId,
      runUid: mainRunId,
      effectiveUserMessage,
    });

    const snapshot = await snapshotBuilder.build({
      sessionId,
      userMessage: message,
      effectiveUserMessage,
    });

    const contextIndex = contextIndexBuilder.build(snapshot);

    const mainDecision = await mainAgent.decideWithSessionIndex({
      userMessage: effectiveUserMessage,
      snapshot,
      contextIndex,
    });

    const validation = decisionValidator.validate({
      decision: mainDecision,
      snapshot,
      contextIndex,
    });

    if (!validation.valid) {
      await updateMainAgentRun(
        mainRunId,
        validation.fallbackDecision.ambiguity?.question || "我需要确认下一步操作。",
        "success"
      );
      return;
    }

    const normalizedDecision = validation.normalizedDecision;

    if (normalizedDecision.decisionType === "ask_user") {
      const question = normalizedDecision.ambiguity?.question || "我需要确认下一步操作。";
      await updateMainAgentRun(mainRunId, question, "success");
      return;
    }

    const plan = planCompiler.compile({
      decision: normalizedDecision,
      snapshot,
      contextIndex,
    });

    switch (plan.mode) {
      case "direct_response": {
        const response = normalizedDecision.response || "我已收到您的消息";
        await updateMainAgentRun(mainRunId, response, "success");

        await markSessionCompleted({
          sessionId,
          runUid: mainRunId,
          effectiveUserMessage,
          summary: response,
        });

        return;
      }

      case "single_agent":
      case "sequential_agents": {
        await updateMainAgentRun(mainRunId, "正在委派任务给子 Agent...", "running");

        await executePlanAsync(
          plan,
          sessionId,
          effectiveUserMessage,
          mainRunId,
          contextIndex
        );

        return;
      }

      default: {
        await updateMainAgentRun(
          mainRunId,
          `暂不支持 plan mode: ${plan.mode}`,
          "failed"
        );

        await markSessionBlocked({
          sessionId,
          runUid: mainRunId,
          effectiveUserMessage,
          error: new Error(`暂不支持 plan mode: ${plan.mode}`),
        });

        return;
      }
    }
  } catch (error) {
    console.error("[processChatAsync] Session-only flow error:", error);

    await markSessionBlocked({
      sessionId,
      runUid: mainRunId,
      effectiveUserMessage,
      error,
    });

    await updateMainAgentRun(
      mainRunId,
      `执行失败：${error instanceof Error ? error.message : "未知错误"}。任务已记录，你可以发送“继续”重试。`,
      "failed"
    );
  }
}

executePlanAsync 里所有 finalWorkContextId 改成 sessionId。

runtime.run 调用从：

workContextId: plan.workContextUid || "",

改成不传 workContextId：

sessionId,
parentRunId: mainRun.id,
mode: "subagent",

不要再传：

workContextId

executePlanAsync 最后的 updateWorkContextProjection 删除，改成 markSessionCompleted / markSessionBlocked。

成功末尾：

await markSessionCompleted({
  sessionId,
  runUid: mainRunId,
  effectiveUserMessage: originalUserMessage,
  summary: finalMessage,
});

await updateMainAgentRun(
  mainRunId,
  finalMessage,
  finalStatus
);

失败分支：

await markSessionBlocked({
  sessionId,
  runUid: mainRunId,
  effectiveUserMessage: originalUserMessage,
  error,
});

await updateMainAgentRun(
  mainRunId,
  `执行失败：${error instanceof Error ? error.message : "未知错误"}。任务已记录，你可以发送“继续”重试。`,
  "failed"
);
============================================================
十二、修改 updateMainAgentRun 函数签名

找到 orchestration.service.ts 里的 updateMainAgentRun。

它现在大概率有 workContextId 参数，并更新 agentRuns.workContextId。

改成：

async function updateMainAgentRun(
  runUid: string,
  resultSummary: string,
  status: string
) {
  const now = nowIso();

  await db
    .update(agentRuns)
    .set({
      status,
      resultSummary,
      updatedAt: now,
      finishedAt: status === "running" ? undefined : now,
    })
    .where(eq(agentRuns.runUid, runUid));

  runEventBus.emitRunStatus(runUid, {
    runId: runUid,
    status,
    resultSummary,
    updatedAt: now,
  });
}

如果 TypeScript 不允许 finishedAt: undefined，拆成：

const patch: any = {
  status,
  resultSummary,
  updatedAt: now,
};

if (status !== "running") {
  patch.finishedAt = now;
}

await db.update(agentRuns).set(patch).where(eq(agentRuns.runUid, runUid));

所有调用点删除第四个参数。

============================================================
十三、修改 AgentRuntime

文件：
agent-platform-node/src/runtime/agent-runtime.ts

当前 AgentRuntime import 了 workContexts，并且 run input 里有 workContextId。它还会在创建 run 后更新 workContexts.currentRunId。

修改：

删除 import：
workContexts
RunAgentInput / RunAgentInputExtended 删除：
workContextId?: string;
创建 run 时删除：
workContextId: input.workContextId,
删除这两段：
if (input.workContextId) {
  await db
    .update(workContexts)
    .set({
      currentRunId: run.id,
      updatedAt: now,
    })
    .where(eq(workContexts.workContextUid, input.workContextId));
}

以及完成后那段更新 workContexts。

所有 artifact 持久化调用从 workContextUid 改成 sessionId。

当前代码里有：

persistArtifactsFromAgentDecisions({
  workContextUid: args.input.workContextId,
  runId: args.runId,
  ...
});

改成：

persistArtifactsFromAgentDecisions({
  sessionId: args.input.sessionId,
  runId: args.runId,
  ...
});

同理修改：

persistDeclaredArtifacts
persistArtifactsFromToolResult
pendingArtifactCandidates 持久化处
============================================================
十四、修改 ArtifactCoordinator

文件：
agent-platform-node/src/modules/artifacts/artifact-coordinator.ts

当前 ArtifactCoordinator 完全依赖 workContextUid，没有 workContextUid 就直接 return。

改成 sessionId。

删除：
import type { CreateArtifactInput } from "../work-contexts/work-context.schema.js";
import { createArtifact } from "../work-contexts/work-context.service.js";
新建本地类型或新的 artifact schema：
type CreateSessionArtifactInput = {
  runId?: number;
  sourceRunId?: number;
  sourceArtifactIds?: string[];
  artifactType: string;
  artifactRole: string;
  title: string;
  mimeType?: string;
  contentText?: string;
  contentJson?: Record<string, unknown>;
  uri?: string;
  status?: string;
  metadata?: Record<string, unknown>;
};
新增 createArtifactForSession：
async function createArtifactForSession(
  sessionId: string,
  input: CreateSessionArtifactInput
) {
  const now = new Date().toISOString();

  const [artifact] = await db
    .insert(agentArtifacts)
    .values({
      artifactUid: makeUid("artifact"),
      sessionId,
      workContextId: null,
      runId: input.runId,
      artifactType: input.artifactType,
      artifactRole: input.artifactRole,
      title: input.title,
      mimeType: input.mimeType,
      contentText: input.contentText ?? "",
      contentJson: jsonStringify(input.contentJson ?? {}),
      uri: input.uri,
      status: input.status ?? "ready",
      sourceRunId: input.sourceRunId,
      sourceArtifactIdsJson: jsonStringify(input.sourceArtifactIds ?? []),
      metadataJson: jsonStringify(input.metadata ?? {}),
      createdAt: now,
      updatedAt: now,
    })
    .returning();

  return artifact;
}

注意需要 import：

import { makeUid } from "../../shared/ids.js";
persistArtifactsFromToolResult 参数改成：
export async function persistArtifactsFromToolResult(params: {
  sessionId?: string;
  runId: number;
  toolResult: ToolResult;
})

内部：

const { sessionId, runId, toolResult } = params;

if (!sessionId) return [];

创建：

const artifact = await createArtifactForSession(sessionId, input);
persistArtifactsFromAgentDecisions 参数改成 sessionId。
persistDeclaredArtifacts 参数改成 sessionId。
savePendingWriteArtifact 参数改成 sessionId。

全部不再使用 workContextUid。

============================================================
十五、修改 fallback-artifact

文件：
agent-platform-node/src/modules/artifacts/fallback-artifact.ts

当前 persistFallbackArtifactFromFinalSummary 需要 workContextUid，并查询 workContexts。

改成：

import { eq } from "drizzle-orm";
import { db } from "../../db/client.js";
import { agentArtifacts } from "../../db/schema.js";
import { jsonStringify } from "../../shared/json.js";
import { makeUid } from "../../shared/ids.js";
import { nowIso } from "../../shared/time.js";

export async function persistFallbackArtifactFromFinalSummary(input: {
  sessionId?: string;
  runId: number;
  summary: string;
  agentName: string;
}) {
  if (!input.sessionId) return;
  if (!input.summary || input.summary.trim().length === 0) return;

  const existingArtifacts = await db
    .select({ id: agentArtifacts.id })
    .from(agentArtifacts)
    .where(eq(agentArtifacts.runId, input.runId))
    .limit(1);

  if (existingArtifacts.length > 0) return;

  const now = nowIso();

  await db.insert(agentArtifacts).values({
    artifactUid: makeUid("artifact"),
    sessionId: input.sessionId,
    workContextId: null,
    runId: input.runId,
    sourceRunId: input.runId,
    artifactType: "text",
    artifactRole: "intermediate",
    title: `${input.agentName} final output`,
    mimeType: "text/plain",
    contentText: input.summary,
    contentJson: "{}",
    status: "ready",
    sourceArtifactIdsJson: "[]",
    metadataJson: jsonStringify({
      generatedBy: "fallback_final_summary",
    }),
    createdAt: now,
    updatedAt: now,
  });
}
============================================================
十六、修改 TaskEnvelopeBuilder / TaskEnvelope types

文件：
agent-platform-node/src/modules/orchestration/task-envelope-builder.ts

当前返回：

workContextUid: plan.workContextUid || "",

改成：

sessionId: plan.sessionId,

如果 TaskEnvelope 类型还没有 sessionId，需要改：

文件：
agent-platform-node/src/modules/orchestration/task-envelope.types.ts

把：

workContextUid: string;

改成：

sessionId: string;

或者兼容期保留：

sessionId: string;
workContextUid?: string;

但新代码不要写 workContextUid。

============================================================
十七、修改 task-envelope-context-hydrator

文件：
agent-platform-node/src/modules/orchestration/task-envelope-context-hydrator.ts

这个文件目前主要按 ref 查 run/artifact/file，不强依赖 WorkContext。

只需要确认 artifact 查询仍按 artifactUid：

.where(eq(agentArtifacts.artifactUid, artifactUid))

这个不用改。

如果 ArtifactSlice 类型有 workContextUid，删除或改为 sessionId。

============================================================
十八、修改 run.service.ts

文件：
agent-platform-node/src/modules/runs/run.service.ts

当前 runAgent 会校验 workContextId：

if (input.workContextId) {
  await getWorkContextByUid(input.workContextId);
}

删除。

删除 import：

import { getWorkContextByUid } from "../work-contexts/work-context.service.js";

runtime.run 调用删除：

workContextId: input.workContextId,

listRuns 可以保留 workContextId 兼容旧接口，但新前端不要传。
建议改成：

export async function listRuns(input?: {
  agentUid?: string;
  sessionId?: string;
  limit?: number;
})

删除 workContextId 分支。

如果要兼容旧查询，可以暂时保留，不影响新链路。

============================================================
十九、修改 run.schema.ts

文件：
agent-platform-node/src/modules/runs/run.schema.ts

删除：

workContextId

RunAgentInput 只保留：

userMessage
handoffNote
mode
sessionId
userId
============================================================
二十、修改 session.service.ts

文件：
agent-platform-node/src/modules/sessions/session.service.ts

当前 getSessionWorkbench 还加载 workContexts，并且 artifacts 是通过 workContextId 查的。

改成完全 Session 版本。

删除 import：

workContexts

getSessionWorkbench 改成：

export async function getSessionWorkbench(sessionUid: string) {
  const session = await getSessionByUid(sessionUid);

  const runList = await db
    .select()
    .from(agentRuns)
    .where(eq(agentRuns.sessionId, sessionUid))
    .orderBy(desc(agentRuns.id))
    .limit(20);

  const artifactList = await db
    .select()
    .from(agentArtifacts)
    .where(eq(agentArtifacts.sessionId, sessionUid))
    .orderBy(desc(agentArtifacts.id))
    .limit(50);

  return {
    session,
    runs: runList,
    artifacts: artifactList,
  };
}

返回里删除：

workContexts
selectedWorkContext
============================================================
二十一、处理 work-context routes/service

文件：
agent-platform-node/src/modules/work-contexts/work-context.routes.ts
agent-platform-node/src/modules/work-contexts/work-context.service.ts

现在可以选择两种做法：

方案 A：保留旧接口，但新流程不用。
给 routes 加 deprecated 注释即可。

方案 B：从 app 注册里移除 registerWorkContextRoutes，彻底关闭接口。

推荐先方案 A：
保留文件，不再调用，不新增数据。

但是要注意：
如果前端还调用 listWorkContexts，就会看到旧数据。
前端要改，不再展示 WorkContext 列表。

============================================================
二十二、修改 Frontend API

文件：
frontend/src/api/agentPlatform.ts

AgentRunRecord 可以保留 workContextId 字段兼容旧数据，但新逻辑不使用。
ChatRequestInput 删除 workContextId：
export type ChatRequestInput = {
  sessionId: string
  message: string
  selectedAgentId?: string
}
ChatResponse 删除 workContextId：
export type ChatResponse = {
  message: string
  runId?: string
  agentId?: string
  artifacts?: Array<{
    id: string
    type: string
    title: string
  }>
}
AgentArtifactRecord 改：
export type AgentArtifactRecord = {
  id: number
  artifactUid: string
  sessionId: string | null
  workContextId: number | null
  runId: number | null
  artifactType: ArtifactType
  artifactRole: ArtifactRole
  title: string
  mimeType: string | null
  contentText: string
  contentJson: string
  uri: string | null
  status: string
  sourceRunId: number | null
  sourceArtifactIdsJson: string
  metadataJson: string
  createdAt: string
  updatedAt: string
}
删除或废弃：
WorkContextRecord
CreateWorkContextInput
listWorkContexts
createWorkContext
getWorkContext
listArtifacts(workContextUid)
createArtifact(workContextUid)
getWorkContextWorkbench
新增按 Session 查 artifacts：

后端也需要新增接口，见下一节。

前端：

async listSessionArtifacts(sessionUid: string) {
  const { data } = await httpClient.get<ApiEnvelope<AgentArtifactRecord[]>>(
    `/sessions/${sessionUid}/artifacts`
  )
  return unwrap(data)
}
getSessionWorkbench 返回类型改：
async getSessionWorkbench(sessionUid: string) {
  const { data } = await httpClient.get<ApiEnvelope<{
    session: SessionRecord
    runs: AgentRunRecord[]
    artifacts: AgentArtifactRecord[]
  }>>(`/sessions/${sessionUid}/workbench`)
  return unwrap(data)
}
============================================================
二十三、新增 Session Artifacts API

文件：
agent-platform-node/src/modules/sessions/session.routes.ts

如果当前没有 /sessions/:sessionUid/artifacts，新增：

app.get<{ Params: { sessionUid: string } }>(
  "/api/agent-platform/sessions/:sessionUid/artifacts",
  async (request) => {
    const session = await getSessionByUid(request.params.sessionUid);

    const artifacts = await db
      .select()
      .from(agentArtifacts)
      .where(eq(agentArtifacts.sessionId, session.sessionUid))
      .orderBy(desc(agentArtifacts.id))
      .limit(100);

    return ok(artifacts);
  }
);

需要 import：

import { desc, eq } from "drizzle-orm";
import { db } from "../../db/client.js";
import { agentArtifacts } from "../../db/schema.js";

或者在 session.service.ts 里做 service 函数：

export async function listArtifactsBySession(sessionUid: string) {
  await getSessionByUid(sessionUid);

  return db
    .select()
    .from(agentArtifacts)
    .where(eq(agentArtifacts.sessionId, sessionUid))
    .orderBy(desc(agentArtifacts.id))
    .limit(100);
}
============================================================
二十四、修改 AgentPlatform.vue

文件：
frontend/src/views/AgentPlatform.vue

需要搜索并删除这些状态：

selectedWorkContext
workContexts
currentWorkContextId
getWorkContextWorkbench
listWorkContexts
createWorkContext
workContextId 传给 chat
workContext tabs/list

聊天发送时，从：

agentPlatformApi.chat({
  sessionId,
  message,
  workContextId: selectedWorkContextId,
})

改成：

agentPlatformApi.chat({
  sessionId,
  message,
})

工作台右侧数据从：

session.workContexts
selectedWorkContext
workContext.artifacts
workContext.runs

改成：

session.runs
session.artifacts
session.metadataJson

UI 文案也改：

“工作上下文” -> “会话上下文”
“当前 WorkContext” -> “当前会话状态”
“WorkContext 列表” -> 删除
============================================================
二十五、更新 Artifact 查询逻辑

任何地方如果按：

agentArtifacts.workContextId

查 artifact，新逻辑都改成：

agentArtifacts.sessionId

保留按 runId 查 artifact 的逻辑，因为 runId 仍然有效。

重点文件：

session.service.ts
work-context.service.ts，如果保留旧接口不用改
fallback-artifact.ts
artifact-coordinator.ts
session-runtime-snapshot-builder.ts
============================================================
二十六、更新 “继续” 逻辑

“继续”不再通过 WorkContext 找，而是通过 sessions.metadataJson 和最近 failed run 找。

流程：

用户：打开百度浏览器
↓
createMainAgentRun 落 agent_runs
↓
patchSessionMetadata:
  lastEffectiveUserMessage = "打开百度浏览器"
  lastRecoverableRunUid = run_xxx
  currentStage = planning
↓
LLM 异常
↓
patchSessionMetadata:
  currentStage = blocked
  lastFailedRunUid = run_xxx
  recoverable = true
↓
用户：继续
↓
resolveEffectiveUserMessage:
  message 是继续
  从 session.metadataJson.lastEffectiveUserMessage 恢复成 "打开百度浏览器"
↓
主 Agent 收到 effectiveUserMessage = "打开百度浏览器"

主 Agent 输入要包含：

{
  "userMessage": "继续",
  "effectiveUserMessage": "打开百度浏览器",
  "sessionState": {
    "currentStage": "blocked",
    "recoverable": true,
    "lastRecoverableRunUid": "run_xxx"
  }
}
============================================================
二十七、清理旧文档/注释

可以暂时不改 docs。
但是代码注释里不能再说：

WorkContext 由 LLM 判断
targetWorkContextUid
createWorkContext
selectedWorkContextUid

尤其是 MainAgent prompt 里必须删干净，否则 LLM 还会输出旧字段。

============================================================