目标：
主 Agent 的常规决策不再使用 refs / relations / contextIndex。
主 Agent 只看：
1. systemPrompt：主 Agent 规则 + availableAgents + MainDecision JSON 合约
2. messages：最近 N 轮 mode=main 的主 Agent 对话历史
3. 当前 user 消息：userMessage + effectiveUserMessage + compactSessionState

删除默认链路中的：
- refs
- relations
- contextIndex 在主 Agent 决策中的依赖
- buildMainDecisionInput()
- truncateRefsForDecision()
- system prompt 中关于 ContextRefs / ContextRelations 的描述
- JSON 合约中“refId 必须来自 refs[].refId”的要求

注意：
- refs / relations 不要从数据库删除。
- SessionContextIndexBuilder 类暂时不要删。
- LedgerReader 暂时不要删。
- TaskEnvelopeBuilder 暂时不要删。
- 子 Agent 的工具调用循环不要改。
1. 修改 model-client.ts

当前 ModelClient.invoke() 已支持 messages，但 complete() 不支持外部传 messages。先把 complete() 改成兼容模式。

文件：

agent-platform-node/src/runtime/model-client.ts

把 complete() 替换成：

async complete(input: {
  systemPrompt: string;
  userMessage?: string;
  messages?: ChatMessage[];
  provider?: string;
  modelName?: string;
  baseUrl?: string | null;
  temperature?: number;
  maxTokens?: number;
  timeoutMs?: number;
}): Promise<{ content: string }> {
  const providerName =
    input.provider || process.env.DEFAULT_MODEL_PROVIDER || "mock";

  const messages: ChatMessage[] = [
    { role: "system", content: input.systemPrompt },
    ...(input.messages ?? [
      { role: "user", content: input.userMessage ?? "" },
    ]),
  ];

  if (providerName === "mock") {
    const result = await this.invoke({
      provider: "mock",
      modelName: "mock-model",
      params: {},
      messages,
      tools: [],
    });

    return { content: result.content };
  }

  const config = getProviderConfig({
    provider: providerName,
    baseUrl: input.baseUrl,
    modelName: input.modelName,
  });

  const result = await this.invoke({
    provider: providerName,
    modelName: input.modelName || config.defaultModel,
    baseUrl: config.baseURL,
    params: {
      temperature: input.temperature ?? 0.7,
      maxTokens: input.maxTokens ?? config.defaultMaxTokens ?? 1000,
      timeoutMs: input.timeoutMs ?? config.timeoutMs,
    },
    messages,
    tools: [],
  });

  return { content: result.content };
}

要求：

旧调用 complete({ systemPrompt, userMessage }) 继续能用。
新调用 complete({ systemPrompt, messages }) 也能用。
2. 新增 main-chat-history-builder.ts

新增文件：

agent-platform-node/src/modules/orchestration/main-chat-history-builder.ts

内容：

import { and, desc, eq, inArray, isNotNull } from "drizzle-orm";
import { db } from "../../db/client.js";
import { agentRuns } from "../../db/schema.js";
import type { ChatMessage } from "../../runtime/model-client.js";

/**
 * 构建主 Agent 的自然对话历史。
 *
 * 只取 mode=main 的 run：
 * - userMessage => user
 * - resultSummary => assistant
 *
 * 不取 subagent run。
 * 不取 running 中间态。
 * 不取 resultSummary 为空的记录。
 */
export async function buildMainChatHistoryMessages(input: {
  sessionId: string;
  limit?: number;
}): Promise<ChatMessage[]> {
  const limit = input.limit ?? 6;

  const runs = await db
    .select({
      userMessage: agentRuns.userMessage,
      resultSummary: agentRuns.resultSummary,
      status: agentRuns.status,
    })
    .from(agentRuns)
    .where(
      and(
        eq(agentRuns.sessionId, input.sessionId),
        eq(agentRuns.mode, "main"),
        inArray(agentRuns.status, ["success", "failed"]),
        isNotNull(agentRuns.resultSummary)
      )
    )
    .orderBy(desc(agentRuns.id))
    .limit(limit);

  return runs
    .reverse()
    .flatMap((run): ChatMessage[] => {
      const messages: ChatMessage[] = [];

      if (run.userMessage?.trim()) {
        messages.push({
          role: "user",
          content: run.userMessage,
        });
      }

      if (run.resultSummary?.trim()) {
        messages.push({
          role: "assistant",
          content: run.resultSummary,
        });
      }

      return messages;
    });
}
3. 修改 main-agent.ts

文件：

agent-platform-node/src/modules/orchestration/main-agent.ts
3.1 修改 import

当前有：

import type { SessionRuntimeSnapshot, SessionContextIndex, MainDecision, MainDecisionInput } from "./orchestration.types.js";

改成：

import type { ChatMessage } from "../../runtime/model-client.js";
import type { SessionRuntimeSnapshot, MainDecision } from "./orchestration.types.js";

如果 SessionContextIndex 其他旧方法还用到，可以暂时保留。但 decideWithSessionIndex 不要再依赖它。

3.2 删除或废弃 buildMainDecisionInput

删除这个方法，或者至少不要再调用：

private buildMainDecisionInput(...)

同时删除或废弃：

private truncateRefsForDecision(...)

因为主 Agent 不再传 refs。

3.3 修改 decideWithSessionIndex

把当前：

async decideWithSessionIndex(input: {
  userMessage: string;
  snapshot: SessionRuntimeSnapshot;
  contextIndex: SessionContextIndex;
}): Promise<MainDecision> {
  ...
  console.log(`[MainAgent] Refs数量: ${input.contextIndex.refs.length}`);

  const systemPrompt = this.buildMainDecisionSystemPrompt();
  const decisionInput = this.buildMainDecisionInput(input);

  const raw = await this.modelClient.complete({
    systemPrompt,
    userMessage: JSON.stringify(decisionInput, null, 2),
    temperature: 1,
  });
  ...
}

改成：

async decideWithSessionIndex(input: {
  userMessage: string;
  snapshot: SessionRuntimeSnapshot;
  mainHistoryMessages?: ChatMessage[];
}): Promise<MainDecision> {
  console.log(`[MainAgent] decideWithSessionIndex 开始`);
  console.log(`[MainAgent] 用户消息: ${input.userMessage.slice(0, 100)}...`);
  console.log(`[MainAgent] SessionId: ${input.snapshot.session.sessionUid}`);
  console.log(`[MainAgent] 历史消息数: ${input.mainHistoryMessages?.length ?? 0}`);

  const systemPrompt = this.buildMainDecisionSystemPrompt(
    input.snapshot.availableAgents
  );

  const currentInput = this.buildCurrentMainDecisionInput(input);

  console.log(`[MainAgent] 当前决策输入: ${JSON.stringify(currentInput, null, 2)}`);

  const raw = await this.modelClient.complete({
    systemPrompt,
    messages: [
      ...(input.mainHistoryMessages ?? []),
      {
        role: "user",
        content: JSON.stringify(currentInput, null, 2),
      },
    ],
    temperature: 0.3,
  });

  const decision = await this.parseMainDecision(raw.content);

  console.log(`[MainAgent] 决策完成: ${decision.decisionType}`);
  console.log(`[MainAgent] Confidence: ${decision.confidence}`);
  console.log(`[MainAgent] Reasoning: ${decision.reasoning.slice(0, 200)}...`);
  console.log(`[MainAgent] 完整决策结果:\n${JSON.stringify(decision, null, 2)}`);

  return decision;
}
3.4 新增 buildCurrentMainDecisionInput

在 MainAgent 类中新增：

private buildCurrentMainDecisionInput(input: {
  userMessage: string;
  snapshot: SessionRuntimeSnapshot;
}) {
  const sessionState = input.snapshot.sessionState;

  return {
    userMessage: input.userMessage,
    effectiveUserMessage: input.snapshot.effectiveUserMessage,
    sessionState: {
      currentStage: sessionState.currentStage,
      recoverable: sessionState.recoverable,
      lastRecoverableRunUid: sessionState.lastRecoverableRunUid,
      lastFailedRunUid: sessionState.lastFailedRunUid,
      lastSuccessfulRunUid: sessionState.lastSuccessfulRunUid,
      recentRefs: sessionState.recentRefs?.slice(0, 5) ?? [],
      openIssues: sessionState.openIssues?.slice(0, 5) ?? [],
    },
  };
}

注意不要放：

lastEffectiveUserMessage

因为它和：

effectiveUserMessage

重复。

3.5 修改 getMainDecisionJsonContract

把里面关于 refs 的强约束删除。

当前类似：

4. primaryRefs、secondaryRefs、plan.steps[].inputRefIds 必须来自输入 refs[].refId。
5. targetAgentUid、plan.steps[].targetAgentUid 必须来自 availableAgents[].agentUid。

改成：

4. 当前常规决策不提供 refs / relations，所以 primaryRefs、secondaryRefs、plan.steps[].inputRefIds 默认输出 []。
5. targetAgentUid、plan.steps[].targetAgentUid 必须来自系统提示中的可用子 Agent 列表。

完整建议改成：

private getMainDecisionJsonContract(): string {
  return `MainDecision JSON 必须包含以下所有顶层字段：

{
  "decisionType": "answer_directly | delegate | multi_step_plan | ask_user | explain_trace | verify_execution | recover_execution",
  "primaryRefs": ["string"],
  "secondaryRefs": ["string"],
  "targetAgentUid": "string or null",
  "plan": {
    "steps": [
      {
        "targetAgentUid": "string",
        "objective": "string",
        "inputRefIds": ["string"],
        "expectedResultKind": "answer | artifact | file_change | diagnosis | verification",
        "requireVerification": true
      }
    ]
  } 或 null,
  "response": "string or null",
  "ambiguity": {
    "candidateRefIds": ["string"],
    "question": "string"
  } 或 null,
  "confidence": "high | medium | low",
  "reasoning": "string"
}

硬性规则：
1. 所有顶层字段都必须存在。
2. 缺失数组字段用 []。
3. 缺失对象字段用 null。
4. 当前常规决策不提供 refs / relations，所以 primaryRefs、secondaryRefs、plan.steps[].inputRefIds 默认输出 []。
5. targetAgentUid、plan.steps[].targetAgentUid 必须来自系统提示中的可用子 Agent 列表。
6. plan 只有执行型决策填写，否则为 null。
7. response 只有 answer_directly 填写，否则为 null。
8. ambiguity 只有 ask_user 填写，否则为 null。
9. 不要输出额外字段。
10. 不要输出 WorkContext 相关字段。`;
}
3.6 修改 buildMainDecisionSystemPrompt

当前 system prompt 说会收到 refs / relations / availableAgents。要删掉 refs / relations，并把 availableAgents 放进 system prompt。

把方法签名改成：

private buildMainDecisionSystemPrompt(
  availableAgents: SessionRuntimeSnapshot["availableAgents"]
): string

完整建议：

private buildMainDecisionSystemPrompt(
  availableAgents: SessionRuntimeSnapshot["availableAgents"]
): string {
  const contract = this.getMainDecisionJsonContract();
  const fallbackExample = this.getAskUserFallbackExample();

  const agentDescriptions = availableAgents
    .map((agent) => {
      const capabilities = agent.capabilities?.length
        ? agent.capabilities.join(", ")
        : "无";
      const description = this.truncateText(agent.description, 200);

      return `- ${agent.agentUid}: ${agent.name}
  描述: ${description}
  能力: ${capabilities}`;
    })
    .join("\n");

  return `你是主 Agent 的结构化决策器，不是执行器。

你会收到：
1. 历史 messages：最近几轮用户与主 Agent 的对话
2. 当前 userMessage：用户本轮原始消息
3. 当前 effectiveUserMessage：系统恢复后的真实任务消息；如果用户说"继续/重试"，这里可能是上一轮失败任务
4. 当前 sessionState：极简会话状态

可用子 Agent：
${agentDescriptions || "无可用子 Agent"}

你必须输出一个合法 MainDecision JSON。
不要输出 Markdown。
不要输出代码块。
不要输出解释性文字。
不要编造不存在的 agentUid。
当前常规决策不提供 refs / relations，因此 primaryRefs、secondaryRefs、plan.steps[].inputRefIds 通常应为空数组。

${contract}

如果无法确定意图，输出这个 ask_user fallback 结构：

${fallbackExample}

决策规则：
1. 如果用户只是问候、普通问答，使用 answer_directly。
2. 如果任务明确且单个 Agent 可以完成，使用 delegate。
3. 如果任务需要多个步骤，使用 multi_step_plan。
4. 如果 userMessage 是"继续/重试/接着"，要优先参考 effectiveUserMessage 和 sessionState。
5. 如果 sessionState.currentStage=blocked 或 recoverable=true，优先考虑 recover_execution。
6. targetAgentUid、plan.steps[].targetAgentUid 必须来自上面的可用子 Agent 列表。
7. 当前常规决策没有加载 refs / relations，所以 primaryRefs、secondaryRefs、plan.steps[].inputRefIds 默认输出 []。
8. answer_directly 必须填写 response。
9. ask_user 必须填写 ambiguity.question。
10. delegate、recover_execution、verify_execution、multi_step_plan 必须填写 plan.steps。
11. reasoning 只写简短内部理由，不超过 300 字。`;
}
4. 修改 orchestration.service.ts

文件：

agent-platform-node/src/modules/orchestration/orchestration.service.ts
4.1 增加 import
import { buildMainChatHistoryMessages } from "./main-chat-history-builder.js";
4.2 修改 processChatAsync

当前大概是：

const snapshot = await snapshotBuilder.build({
  sessionId,
  userMessage: message,
  effectiveUserMessage,
});

const contextIndex = contextIndexBuilder.build(snapshot);

const mainDecision = await mainAgent.decideWithSessionIndex({
  userMessage: message,
  snapshot,
  contextIndex,
});

改成：

const mainHistoryMessages = await buildMainChatHistoryMessages({
  sessionId,
  limit: 6,
});

const snapshot = await snapshotBuilder.build({
  sessionId,
  userMessage: message,
  effectiveUserMessage,
});

const mainDecision = await mainAgent.decideWithSessionIndex({
  userMessage: message,
  snapshot,
  mainHistoryMessages,
});

这里直接删除：

const contextIndex = contextIndexBuilder.build(snapshot);

因为主 Agent 决策不再用它。

4.3 但后续执行计划还需要 contextIndex

注意：当前后面还有：

executePlanAsync(
  plan,
  sessionId,
  effectiveUserMessage,
  mainRunId,
  contextIndex
)

如果你完全删除 contextIndex，这里会报错。

最小兼容改法是：

只有当进入执行计划时，再构建 contextIndex：

case "single_agent":
case "sequential_agents": {
  await updateMainAgentRun(
    mainRunId,
    "正在委派任务给子 Agent...",
    "running"
  );

  const contextIndex = contextIndexBuilder.build(snapshot);

  await executePlanAsync(
    plan,
    sessionId,
    effectiveUserMessage,
    mainRunId,
    contextIndex
  );

  return;
}

也就是说：

主 Agent 决策阶段不构建 contextIndex。
只有真正要 executePlanAsync 时，才构建 contextIndex。

这样能删掉主 Agent LLM 的 refs / relations 输入，同时不影响后续 TaskEnvelopeBuilder 的入参。

5. 修改 decision-contract-validator.ts

这个地方很关键。

当前 validator 会校验：

primaryRefs 必须存在于 contextIndex.refs
secondaryRefs 必须存在于 contextIndex.refs
plan.steps[].inputRefIds 必须存在于 contextIndex.refs

但现在主 Agent 常规决策没有 refs，模型默认输出 []。

如果模型乖乖输出 []，没问题。

为了更稳，你可以保留校验，但要保证空数组不会失败。

如果代码里要求 contextIndex 必传，暂时可以继续在 validation 前构建一个空 contextIndex：

const emptyContextIndex = {
  refs: [],
  relations: [],
};

更推荐改 validate() 支持轻量模式：

validate(input: {
  decision: MainDecision;
  snapshot: SessionRuntimeSnapshot;
  contextIndex?: SessionContextIndex;
})

然后：

const validRefIds = new Set(input.contextIndex?.refs.map((ref) => ref.refId) ?? []);

这样空 refs 下，只有模型输出了非空 refId 才失败。

validAgentUids 继续来自：

input.snapshot.availableAgents
6. processChatAsync 里的 validation 怎么改

当前应该是：

const validation = decisionValidator.validate({
  decision: mainDecision,
  snapshot,
  contextIndex,
});

改成：

const validation = decisionValidator.validate({
  decision: mainDecision,
  snapshot,
  contextIndex: {
    refs: [],
    relations: [],
  },
});

或者如果你按上面改了 validator 支持可选：

const validation = decisionValidator.validate({
  decision: mainDecision,
  snapshot,
});

更推荐后者。

7. planCompiler.compile 怎么处理

当前 compile 需要：

planCompiler.compile({
  decision: normalizedDecision,
  snapshot,
  contextIndex,
});

它目前主要用 contextIndex 计算 allowedTools，但 computeAllowedTools 现在实际返回空数组：

return [];

所以这里可以改成：

const emptyContextIndex = {
  refs: [],
  relations: [],
};

const plan = planCompiler.compile({
  decision: normalizedDecision,
  snapshot,
  contextIndex: emptyContextIndex,
});

或者更进一步，把 ExecutionPlanCompiler.compile() 里的 contextIndex 改成可选。

最小改法：

const emptyContextIndex = { refs: [], relations: [] };

const validation = decisionValidator.validate({
  decision: mainDecision,
  snapshot,
  contextIndex: emptyContextIndex,
});

const plan = planCompiler.compile({
  decision: normalizedDecision,
  snapshot,
  contextIndex: emptyContextIndex,
});

只有 executePlanAsync 前再构建真实 contextIndex。

8. 推荐 processChatAsync 最终结构

请把主流程调整成这个思路：

const mainHistoryMessages = await buildMainChatHistoryMessages({
  sessionId,
  limit: 6,
});

const snapshot = await snapshotBuilder.build({
  sessionId,
  userMessage: message,
  effectiveUserMessage,
});

const mainDecision = await mainAgent.decideWithSessionIndex({
  userMessage: message,
  snapshot,
  mainHistoryMessages,
});

const emptyContextIndex = { refs: [], relations: [] };

const validation = decisionValidator.validate({
  decision: mainDecision,
  snapshot,
  contextIndex: emptyContextIndex,
});

...

const plan = planCompiler.compile({
  decision: normalizedDecision,
  snapshot,
  contextIndex: emptyContextIndex,
});

switch (plan.mode) {
  case "direct_response":
    ...
    return;

  case "single_agent":
  case "sequential_agents": {
    await updateMainAgentRun(...);

    const executionContextIndex = contextIndexBuilder.build(snapshot);

    await executePlanAsync(
      plan,
      sessionId,
      effectiveUserMessage,
      mainRunId,
      executionContextIndex
    );

    return;
  }
}

这样：

主 Agent 决策：不吃 refs / relations
执行 TaskEnvelope：仍然可以用 contextIndex
9. 最后要检查的旧代码
9.1 MainDecisionInput 类型

orchestration.types.ts 里如果 MainDecisionInput 只被旧 buildMainDecisionInput() 使用，可以删掉。

如果还有引用，先保留，不影响。

9.2 truncateRefsForDecision

如果没有引用，删掉。

9.3 buildMainDecisionInput

如果没有引用，删掉。

9.4 decideWithSessionIndex 名字

虽然现在已经不接收 contextIndex 了，但方法名叫 decideWithSessionIndex 不太准确。

可以先不改名，减少改动。

后续再重命名成：

decideMain()

或：

decideWithChatHistory()
10. 验收标准

改完后要满足：

1. TypeScript 编译通过。
2. MainAgent 决策日志里不再出现 Refs数量。
3. MainAgent 决策输入日志里不再出现 refs。
4. MainAgent 决策输入日志里不再出现 relations。
5. 普通任务可以正常 answer_directly / delegate。
6. 多轮主对话历史来自 agent_runs where mode=main。
7. 当前 running 的 main run 不会进入历史，因为它不是 success/failed 且 resultSummary 为空或中间态。
8. 子 Agent 工具调用不受影响。
9. executePlanAsync 前仍然能构建 contextIndex 给 TaskEnvelopeBuilder 使用。
10. 模型输出 primaryRefs / secondaryRefs / inputRefIds 为空数组时 validation 能通过。

最终一句话：

主 Agent 决策阶段彻底删掉 refs / relations。
只保留主对话历史 + 当前任务 + sessionState + availableAgents。
contextIndex 延迟到真正执行子 Agent 前再构建。